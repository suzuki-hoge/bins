# Git Commit Amend ( gcma )

## gcma

alias to ***g***it ***c***o***m***mit ***a***mend.

```
$ gcma -h
bins 0.1.0

USAGE:
    git-commit-amend

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information
```

examples.

``` 
$ gcma
```
